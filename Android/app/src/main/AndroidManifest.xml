<?xml version="1.0" encoding="utf-8"?>
<!-- Android/app/src/main/AndroidManifest.xml -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".XtreamPlayerApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.XtreamPlayer"
        android:usesCleartextTraffic="true">

        <!--
            AdMob application id. Sourced from the manifestPlaceholders["admobAppId"]
            entry in app/build.gradle.kts (currently Google's official PUBLIC TEST id
            ca-app-pub-3940256099942544~3347511713).
            TODO: MUST be replaced with a real AdMob app id before release - the test
            id is safe to ship in debug but will not serve real ads / will violate
            AdMob policy in production.
        -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="${admobAppId}" />

        <activity
            android:name=".presentation.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.XtreamPlayer"
            android:screenOrientation="unspecified">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
